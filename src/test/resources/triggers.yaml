triggers:
  # 1) ON_CREATE :Person -> print "A"
  - id: person-create
    scope: NODE
    event: ON_CREATE
    labels: [Person]
    priority: 100
    enabled: true
    action:
      type: stdout
      message: "A node Person has been created and seen by the trigger person-create"

  # 2) ON_DELETE :Person -> print "B"
  - id: person-delete
    scope: NODE
    event: ON_DELETE
    labels: [Person]
    priority: 100
    enabled: true
    action:
      type: log
      message: "B"